#getch, getche

getch, getche는 굉장히 독특합니다.

문자를 가져오는 함수이지만, stdin을 통해서 가져오는게 아니라

커널에서 바로 문자를 요청해 가져옵니다.

즉, 버퍼를 거치지 않고 우선적으로 정보를 수령해 옵니다.

버퍼를 거치지 않기때문에 stdio.h가 아닌 conio.h에 선언되어 있습니다.

getch는 문자를 가져와서 그대로 반환하고,
getche는 getch에 더해서 가져온 문자를 출력합니다.

받는 인자는 없습니다.
